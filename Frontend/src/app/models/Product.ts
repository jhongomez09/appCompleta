export interface Product {
    _id?: string;
    name: string;
    price: string;
    category: string;
    brand: string;
    imgUrl: string;
    createdAt?: string;
    updatedAt?: string;
    /* ? opcional */
}